# Longhorn

Recursos relacionados con el almacenamiento persistente del cluster.

| Archivo | Contenido |
| --- | --- |
| `storageclass-longhorn.yaml` | StorageClass `longhorn` exportada desde el cluster real |

Longhorn se utiliza para los volumenes persistentes de Odoo, PostgreSQL y Grafana.
